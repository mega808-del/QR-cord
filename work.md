# 📇 연락처 QR코드 생성기 — 작업 내역

> 마지막 업데이트: 2026-09-18
> 프로젝트 위치: `C:\Users\HeungPC\Desktop\Project\QR cord`

---

## 1. 프로젝트 개요

이름과 휴대폰 번호만 입력하면 **갤럭시/아이폰 공용 vCard 3.0 QR코드**를 생성하는 웹 프로그램.
생성된 QR은 **다운로드**하거나 **공유 링크**로 전달할 수 있고, 받은 사람은 모바일에서 원터치 저장이 가능하다.

- 스택: Python 3.14.7 + Flask 3.1.3 + qrcode 8.2 + pillow 11.3.0 (검증: OpenCV 5.0.0)
- 실행 환경: Windows 10/11 (콘솔 인코딩 CP949 주의)

---

## 2. 파일 구성

| 파일 | 용도 | 상태 |
|---|---|---|
| `app.py` | **메인 웹 서버** (QR 생성·공유 링크·다운로드) | ✅ 완성 |
| `start.bat` | 더블클릭 실행용 배치 파일 | ✅ 완성 (ASCII+CRLF) |
| `generate_contact_qr.py` | 초기 단발성 스크립트 (홍길동 고정) | 보존 |
| `verify_app.py` | 웹앱 자동 검증 테스트 (27개 항목) | ✅ 27/27 통과 |
| `verify_qr.py` | 초기 QR 디코딩 검증 스크립트 | 보존 |
| `contact_qr_common.png` | 초기 생성 QR 이미지 | 보존 |
| `saved_qrcodes/` | 생성된 QR 서버 백업 폴더 (자동 저장) | 자동 생성 |

---

## 3. 작업 히스토리

### 1단계 — 초기 스크립트 실행
- 사용자가 제공한 vCard QR 생성 스크립트를 `generate_contact_qr.py`로 저장 후 실행
- `qrcode` 모듈 미설치 → `pip install qrcode pillow`로 해결
- `contact_qr_common.png` 생성 성공

### 2단계 — 정상 동작 분석
- OpenCV `QRCodeDetector`로 PNG 디코딩 → **데이터 무결성 100% 일치** 확인
- 발견 사항: `version=1`로 지정했지만 한글 UTF-8(90바이트) 때문에 실제로는 **QR 버전 6**으로 생성됨 (`fit=True`가 자동 승격)
- 개선 제안: `version` 인자 제거, 콘솔 UTF-8 출력 설정

### 3단계 — 웹 앱으로 업그레이드 (`app.py`)
- Flask 기반: 이름/번호 입력 → QR 생성 → 다운로드/공유
- 번호 자동 정규화 (`010-2345-6789`, `010 2345 6789`, `01023456789` 모두 허용 → `01023456789`로 저장)
- 서버 측 입력 검증 (빈 값, 잘못된 번호, 줄바꿈 주입/vCard 변조 차단, 이름 30자 제한)
- 다크모드 지원, 모바일 반응형 UI
- `verify_app.py` 작성 → Flask test client + OpenCV 디코딩 검증

### 4단계 — 공유 링크 기능 추가 (상대방 원터치 저장)
- QR 생성 시 서버 메모리에 등록(`QR_STORE`, 최대 100개 LRU) → 공유 링크 발급
- `GET /s/<id>` : 받은 사람용 모바일 저장 페이지 (큰 [⬇️ 이미지 저장] 버튼, "길게 눌러 저장" 안내, 재공유 버튼)
- `GET /qr/<id>` : PNG 다이렉트 다운로드 (`attachment` 헤더)
- 공유 페이지에는 QR이 base64 data URI로 내장 (별도 로딩 없음)
- 서버 시작 시 PC 내부 IP 자동 감지 → 스마트폰 접속용 주소 안내 (`http://192.168.x.x:5000`)
- `app.run(host="0.0.0.0")` 으로 LAN 접속 허용
- 검증: **27/27 통과** (공유 페이지 내장 QR 무결성 포함)

### 5단계 — 더블클릭 실행 (`start.bat`)
- app.py에 브라우저 자동 열기 추가 (기동 1초 후, `QR_NO_BROWSER=1`로 비활성 가능 — 자동 테스트용)
- `start.bat` 생성

### 6단계 — start.bat 인코딩 버그 수정 ⚠️ 중요 학습
- **증상**: 더블클릭 시 `釉뚮씪?곗?媛...` 같은 깨진 문자가 "내부/외부 명령이 아닙니다" 에러 발생
- **원인**: bat 파일이 UTF-8 한글 포함으로 저장됨 → `cmd.exe`는 배치 파일을 **CP949**로 읽기 때문에 한글 주석이 깨져 명령어로 오인 실행됨
- **진단 방법**: 파일을 CP949로 강제 디코딩해 동일한 깨진 문자 재현 확인
- **해결**: start.bat을 **영문(ASCII) 전용 + CRLF 줄바꿈**으로 재작성
  - `python` 없으면 `py` 런처 폴백, 둘 다 없으면 설치 안내
  - 콘솔 메시지는 영문 (웹 화면은 한글 유지)
- **실전 테스트** (`cmd //c start.bat` 실행):
  - 깨진 명령 에러 없음 ✅
  - GET / → 200, POST /api/qr → 200 ✅
  - QR 디코딩 무결성 PASS ✅
  - LAN 주소 `http://192.168.219.109:5000` 자동 표시 ✅

---

## 4. 실행 방법

```
더블클릭:  start.bat  (브라우저 자동 실행)
또는
명령어:   cd "C:\Users\HeungPC\Desktop\Project\QR cord"
          python app.py
```

- PC 접속: `http://127.0.0.1:5000`
- 스마트폰 접속 (같은 Wi-Fi): 콘솔에 표시되는 `http://192.168.x.x:5000`
  - 첫 접속 시 Windows 방화벽 허용 필요
- 종료: 콘솔 창 닫기 또는 Ctrl+C

## 5. API / 라우트

| 라우트 | 메서드 | 기능 |
|---|---|---|
| `/` | GET | 메인 입력 페이지 |
| `/api/qr` | POST | QR 생성 (JSON: `{name, phone}` → PNG + `X-Qr-Id` 헤더) |
| `/s/<id>` | GET | 공유 링크 랜딩 페이지 (모바일 저장 UI + 앱 설치 버튼) |
| `/qr/<id>` | GET | QR PNG 다이렉트 다운로드 |
| `/manifest.webmanifest` | GET | PWA 매니페스트 (앱 이름: 연락처QR, standalone) |
| `/icon-192.png` / `/icon-512.png` | GET | PWA 앱 아이콘 (PIL 동적 생성, 캐시) |
| `/icon-maskable.png` | GET | PWA maskable 아이콘 (안드로이드 적응형) |
| `/sw.js` | GET | 서비스 워커 (정적 아이콘 캐시 전용) |

## 6. 현재 제약 및 다음 작업 후보

- [ ] 공유 링크 만료 기능 (현재 서버 재시작 시 초기화, 메모리 최대 100개)
- [ ] 공유 링크를 파일 기반으로 영구 저장
- [ ] 포트 5000 충돌 시 자동 대체 포트
- [ ] vCard 필드 확장 (회사/이메일/메모)
- [ ] CSV 대량 생성 기능
- [ ] 콘솔 한글 표시가 필요하면 .pyw 실행기 검토 (bat은 CP949 제약)
### 7단계 — PWA 모바일 앱 설치 기능 추가 (2026-09-18)

**7-1. 공유 페이지 설치 버튼**
- 공유 링크 페이지에 **[📱 앱으로 설치 (바탕화면 추가)]** 버튼 추가 (공유 버튼 우측 하단)
- 클릭 시: Chrome 등 → 네이티브 설치 프롬프트(`beforeinstallprompt`) / 미지원 브라우저 → 설치 안내 모달
- 설치 시 모바일 바탕화면에 **"연락처QR"** 제목 + 파란 그라디언트 📇 아이콘으로 standalone 앱 실행
- 앱 아이콘: PIL로 동적 생성 (`/icon-192.png`, `/icon-512.png`, `/icon-maskable.png`) — 이모지 폰트 탐색 실패 시 "QR" 카드 그래픽 폴백
- 메인 페이지에도 PWA 메타 + SW 등록 주입 → 메인 페이지에서도 설치 가능
- 서비스 워커는 **정적 아이콘 캐시 전용** (페이지/QR은 항상 네트워크 — 오래된 QR 표시 방지)
- 기존 기능 전부 유지, `verify_app.py`에 PWA 검증 13항목 추가 (총 40항목)
- GitHub 업로드용 `.gitignore` 추가 (`__pycache__/`, `saved_qrcodes/` 등)

**7-2. 메인 페이지 결과 UI 개편**
- QR 생성 성공 시 **입력폼을 숨기고** 결과(QR + 설치/공유 버튼 + 공유 링크)만 표시
- 결과 화면 버튼 구성: 1행 [📱 앱으로 설치] [🔗 공유] / 2행 [⬇️ 이미지 저장] [↺ 새로 만들기]
- "새로 만들기" 클릭 시 입력폼으로 복귀 (기존 입력값 유지)
- 메인 페이지에도 설치 안내 모달 추가 → 어느 화면에서든 원클릭/안내 설치 가능

### 8단계 — PWA 설치 요건 검증 (2026-09-18)
- `verify_pwa.py` 신규 작성: 실행 중인 서버에 실제 HTTP 접속해 설치 필수 요건 20항목 검증 → **20/20 통과**
  - 매니페스트 (standalone, 192/512/maskable 아이콘, start_url), 아이콘 PNG 유효성(PIL 재파싱), sw.js, 설치 버튼
- Chrome(headless) 실제 페이지 로드 확인: 설치 버튼·매니페스트·SW 등록 코드 존재, JS 오류 없음
- 헤드리스 환경 한계: `beforeinstallprompt` 발생은 사용자 클릭+실제 브라우저 컨텍스트가 필요해 재현 불가 → 실제 설치는 배포(HTTPS) 후 모바일에서 최종 확인 필요

### 9단계 — 바탕화면 바로가기 복원 기능 (2026-09-18)
- **요구**: 바탕화면의 "연락처QR" 아이콘으로 열면 입력폼 없이 생성된 QR이 바로 나오게 할 것
- 구현:
  - QR 생성 시 `last_qr.json` 파일로 영속 저장 (`save_last_qr()`) — **서버 재시작 후에도 복원 가능**
  - 신규 API `GET /api/last-qr` — 마지막 QR(name/phone/qid/PNG base64) 반환
  - 메인 페이지 로드 시 즉시 조회 → 있으면 `showRestoredQR()`로 입력폼 숨기고 QR 화면 표시
  - 복원 상태에서 [⬇️ 이미지 저장]은 `/qr/<id>` 경유로 서버에서 받아와 저장 (blob 없는 상태 대응)
  - [↺ 새로 만들기]로 언제든 입력폼 복귀
- 검증: `verify_shortcut.py` 신규 — **2회 반복 시나리오 테스트 31/31 통과**
  - 1회차(이상호/01056630101), 2회차(홍길동/010-2345-6789 정규화 포함)
  - 생성 → 페이지 재접속 시 복원 표시 → 복원 QR OpenCV 디코딩 무결성 → 다운로드 → 파일 저장 확인
- 발생했던 이슈: 포트 5000에 **옛 서버 프로세스 6개가 누적 점유**되어 새 코드가 반영되지 않았던 것(404) → `netstat -ano` + `taskkill //F //PID`로 정리 후 해결. 서버 코드 수정 후 테스트 시 포트 점유 프로세스 정리 필수

### 10단계 — 9단계 기능 되돌림 (2026-09-18) ⏪
- **사유**: 바로가기로 열 때 마지막 QR(테스트용 홍길동)이 자동 표시되는 것이 오히려 혼란 — "입력 → 생성 버튼을 눌러야 QR이 나오는" 원래 동작이 맞음
- 제거: 자동 복원 JS(`restoreLastQR`/`showRestoredQR`), `GET /api/last-qr` 라우트, `last_qr.json` 저장 로직, `verify_shortcut.py`, `last_qr.json` 파일
- 유지: 생성 후 폼 숨김 + 결과 중심 UI, 설치/공유/다운로드/새로 만들기 버튼 (7단계 UI는 그대로)
- 재검증: verify_app **44/44 통과**, verify_pwa **PASS**
- 교훈: `last_qr.json` 영속 저장 방식은 추후 "QR 다중 저장/선택" 기능의 기반으로 재활용 가능

### 11단계 — 설치 버튼 원터치 개선 (2026-09-18)
- **요구**: 앱 설치 버튼 누르면 모바일 바탕화면에 바로 설치되게
- **브라우저 보안 정책상 완전 자동 설치(터치 0번)는 불가능** — 설치 최종 확인은 사용자 터치 1번 필요 (Chrome/Samsung Internet 공통)
- 개선한 것:
  - 클릭 시 설치 팝업(`beforeinstallprompt`)이 아직 안 온 경우 **최대 2.5초 대기** 후 팝업 표시 → 팝업 타이밍 이슈로 안내 모달로 빠지는 경우 감소
  - 설치 최종 확인 터치 1번 → 설치 완료 → `appinstalled` 이벤트로 **"✅ 설치 완료" 안내 표시**
  - 설치 취소 시 재시도 안내, iOS(사파리)는 공유 버튼 → 홈 화면에 추가 전용 안내 분기(`stepIos`)
  - 메인/공유 페이지 모두 동일 적용
- 검증: verify_app **50/50 통과** (신규 6항목 추가), Chrome 실로드에서 신규 로직 6곳 확인
- 남은 한계: 네이티브 설치 팝업 자체는 **HTTPS(또는 localhost)에서만** 브라우저가 허용 — LAN(IP) 접속 시 안내 모달 경로. GitHub Pages 등 HTTPS 배포로 해결

### 12단계 — 앱 아이콘 클릭 시 마지막 QR 복원 (재구현, 2026-09-18)
- **요구**: 설치된 "연락처QR" 앱 아이콘 클릭 시 입력폼 대신 생성된 QR이 바로 표시되게
- 9단계에서 구현했다가 10단계에서 되돌렸던 기능을 재구현. 차이점:
  - **테스트 데이터 정리 절차 추가** — 검증 후 `last_qr.json`을 삭제해 사용자 앱에 테스트 QR(홍길동)이 표시되는 문제 차단 (10단계에서 되돌린 원인)
  - 복원 시 상태 표시에 "[새로 만들기]로 다른 QR을 만들 수 있어요" 안내 추가
  - `.gitignore`에 `last_qr.json` 추가 (이름/번호가 담긴 개인정보가 GitHub에 노출되지 않도록)
- 복구 흐름: QR 생성 → `last_qr.json` 영속 저장 → 앱/페이지 열 때 `/api/last-qr` 조회 → 있으면 입력폼 숨기고 QR 즉시 표시
- 검증: `verify_shortcut.py` 재작성 — **2회 시나리오 31/31 통과**, verify_app **50/50 PASS**
- 유의: 서버 코드 수정 후 재테스트 시 **포트 5000 옛 프로세스 종료 필수** (10단계 이슈 재발 방지)

## 10. GitHub 업로드 참고

- `.gitignore`로 `__pycache__/`, `saved_qrcodes/` 제외됨
- 공유 링크는 서버 메모리 기반이므로 **배포 환경(재시작/멀티 프로세스)에서 링크 유지되지 않음** — 필요 시 파일 기반 저장으로 확장
- PWA 설치 프롬프트는 HTTPS 또는 localhost에서만 활성화됨. LAN(IP) 접속 시에는 안내 모달 경로로 동작

## 7. 검증 방법 (재실행용)

```bash
python verify_app.py       # 44개 항목 자동 검증 (라우팅/무결성/입력차단/공유링크/PWA)
python verify_pwa.py       # 20개 항목 검증 — 서버 실행 중에 PWA 설치 요건 확인
```
